# Experimentos

Área para protótipos isolados de loading, textura, halftone, transições, scroll e WebGL.

Cada experimento deve registrar hipótese, referência, custo observado, comportamento mobile, fallback e decisão final antes de migrar para `app/`.
